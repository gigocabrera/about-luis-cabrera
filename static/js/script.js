const nav=document.getElementById('siteNav');const brandLogo=document.getElementById('brandLogo');const navToggle=document.getElementById('navToggle');const navLinks=document.getElementById('navLinks');const lightLogo='static/img/LuisCabreraLogoBlue.svg';const darkLogo='static/img/LuisCabreraLogoWhite.svg';function syncNav(){const scrolled=window.scrollY>35;nav.classList.toggle('scrolled',scrolled);if(brandLogo)brandLogo.src=scrolled?darkLogo:lightLogo}window.addEventListener('scroll',syncNav,{passive:true});syncNav();if(navToggle&&navLinks){navToggle.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open))});navLinks.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{navLinks.classList.remove('open');navToggle.setAttribute('aria-expanded','false')}))}const revealEls=document.querySelectorAll('.reveal');if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.1,rootMargin:'0px 0px -40px'});revealEls.forEach(el=>observer.observe(el))}else{revealEls.forEach(el=>el.classList.add('is-visible'))}document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',event=>{const id=link.getAttribute('href');const target=document.querySelector(id);if(!target)return;event.preventDefault();const top=id==='#top'?0:target.getBoundingClientRect().top+window.scrollY-(nav?.offsetHeight||0);window.scrollTo({top:Math.max(0,top),behavior:'smooth'});if(id!=='#top')history.replaceState(null,'',id)}));
